apiVersion: apps/v1
kind: Deployment
metadata:
  name: receipt-service
spec:
  selector:
    matchLabels:
      app: receipt-service

  template:
    metadata:
      labels:
        app: receipt-service
    spec:
      containers:
        - name: receipt-service
          image: {{ .Values.receiptService.image.repository }}:{{ .Values.receiptService.image.tag }}
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.receiptService.port }}
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.receiptService.port }}
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.receiptService.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: finance-secrets
                  key: anthropic-api-key
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: finance-secrets
                  key: jwt-secret
            - name: ALLOWED_ORIGINS
              value: "https://allexpense.me,https://www.allexpense.me"
