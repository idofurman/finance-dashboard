server {
    listen 80;
    root /usr/share/nginx/html;

    location = / {
        try_files /login.html =404;
    }

    location = /app {
        try_files /index.html =404;
    }

    location / {
        try_files $uri =404;
    }
}
